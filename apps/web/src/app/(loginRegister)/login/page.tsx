import LoginForm from "@/app/(loginRegister)/(components)/LoginForm";
import { FC } from "react";

export const metadata = {
	title: "124sdfgh234",
};

const Login: FC = () => {
	return (
		<div className="px-6 py-10">
			<LoginForm />
			
		</div>
	);
};

export default Login;
